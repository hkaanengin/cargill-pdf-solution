[pytest]
# `pythonpath = .` puts the repo root on sys.path so `import stamper` resolves
# from tests/ — the module is a flat file at the root, not an installed package
# (vault/decisions/0012-module-shape-and-test-tooling.md).
pythonpath = .
testpaths = tests
